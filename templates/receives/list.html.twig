{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        <span class="glyphicon glyphicon-th-large"></span> Administração de contas a receber
                    </h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-12">
                            <a href="{{ route('receives.new') }}" class="btn btn-default">
                                <span class="glyphicon glyphicon-plus"></span> Novo lançamento
                            </a>
                        </div>
                    </div>
                    <br/>
                    <div class="row">
                        <div class="col-md-12">
                            <table class="table table-bordered">
                                <thead>
                                <tr>
                                    <th class="col-md-1 text-center">ID</th>
                                    <th class="col-md-1 text-center">LANÇAMENTO</th>
                                    <th class="col-md-7 text-center">DESCRIÇÃO</th>
                                    <th class="col-md-1 text-center">VALOR</th>
                                    <th class="col-md-1 text-center">STATUS</th>
                                    <th class="col-md-1 text-center">AÇÕES</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for receive in receives %}
                                    <tr>
                                        <td class="text-center">{{ receive.id }}</td>
                                        <td>{{ receive.date_launch | date('d/m/Y') }}</td>
                                        <td>{{ receive.name }}</td>
                                        <td>{{ receive.value | number_format(2,',','.') }}</td>
                                        <td>{{ receive.status == 1 ? 'Aberto' : 'Fechado' }}</td>
                                        <td class="text-center">
                                            <a href="{{ route('receives.edit',{'id': receive.id}) }}" class="btn btn-sm btn-default">
                                                <span class="glyphicon glyphicon-pencil"></span>
                                            </a>
                                            <a href="{{ route('receives.show',{'id': receive.id}) }}" class="btn btn-sm btn-default">
                                                <span class="glyphicon glyphicon-search"></span>
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}